# AGENTS.md

이 저장소에서 코드를 고칠 때 지키는 규칙.

## 커밋 메시지

**제목 1줄, 본문 3줄 이하.** 본문이 길어지면 그건 커밋이 아니라 주석이나
`docs/` 로 갈 내용이다.

```
type(scope): 무엇을 하는지 한국어 평서형으로 (~한다)

왜 필요했는지와 무엇이 바뀌는지. 세 줄을 넘기지 않는다. 넘어가면 주석으로
옮기고 여기서는 그 파일을 가리킨다.
```

- `type` 은 `feat` / `fix` / `refactor` / `chore` / `docs` 중 하나.
- `scope` 는 건드린 자리(`hyprland`, `rice`, `packages`, `darwin`, `fonts`…).
- 제목은 명사구가 아니라 **동작**으로 쓴다 — "포커스 버그" 가 아니라
  "그 창으로 포커스가 가게 한다".
- 한 커밋은 한 가지 일만 한다. 한 파일 안에서 서로 다른 일을 했으면 hunk 로
  갈라서 따로 커밋한다.
- 중간 커밋도 각각 성립해야 한다. 함수 이름을 바꾸면 부르는 쪽까지 같은
  커밋에 넣는다.

## 주석

**메커니즘과 결과는 남기고, 사건 서술은 뺀다.** 주석은 다음에 이 코드를 읽는
사람을 위한 것이지 무슨 일이 있었는지에 대한 기록이 아니다. 그건 git log 가
한다.

빼야 하는 것:

- "실제로 이틀 넘게 그 상태였다", "한 번 데였다", "날아갈 뻔했다" — 사건 서술
- "예전에는 X 였고 그래서 Y 로 바꿨다" — 지운 코드의 부고. 남은 코드가 왜
  지금 모양인지만 적는다.
- 커밋 하나에서만 의미 있는 경위. 그건 커밋 메시지에 세 줄로 적는다.

남겨야 하는 것:

- 이 줄이 없으면 무엇이 깨지는지, 그리고 그 증상이 어떻게 보이는지.
  특히 **조용히 실패하는** 것은 반드시 적는다.
- 왜 더 뻔한 방법을 안 썼는지 (`require` 가 아니라 `dofile` 인 이유 같은 것).
- 상류의 사실과 그 출처 (`Modules/Settings/ThemeColorsTab.qml` 의
  `grepPattern` 처럼 파일·심볼까지).
- 값을 실측했으면 그 수치와 측정 조건.

`docs/postmortems/` 는 예외다 — 거기는 사건 기록이 본문이다.
