diff --git a/Sources/WallpaperPeek/HotkeyListener.swift b/Sources/WallpaperPeek/HotkeyListener.swift
index b112c4a..4ae7fcf 100644
--- a/Sources/WallpaperPeek/HotkeyListener.swift
+++ b/Sources/WallpaperPeek/HotkeyListener.swift
@@ -9,8 +9,13 @@ final class HotkeyListener {
     private var eventTap: CFMachPort?
     private var runLoopSource: CFRunLoopSource?
 
+    // 로컬 변경: Option+Q -> Option+Control+Q.
+    // Option+Q 는 rift 의 close_window 와 겹친다(modules/darwin/config/rift.toml).
+    // 피커들은 Alt+Ctrl 계열로 모아 뒀다 — Alt+Ctrl+Q 가 여기, Alt+Ctrl+W 가
+    // WorkspacePeek. 아래 handle() 은 flags 를 == 로 정확히 비교하므로 수식자를
+    // 더해도 다른 조합에 걸리지 않는다.
     private let triggerKeyCode: CGKeyCode = 0x0C  // kVK_ANSI_Q
-    private let triggerModifiers: CGEventFlags = [.maskAlternate]  // Option
+    private let triggerModifiers: CGEventFlags = [.maskAlternate, .maskControl]  // Option+Control
 
     func start() {
         let mask: CGEventMask =
diff --git a/Sources/WallpaperPeek/WallpaperEngine.swift b/Sources/WallpaperPeek/WallpaperEngine.swift
index 35f3106..b1eab45 100644
--- a/Sources/WallpaperPeek/WallpaperEngine.swift
+++ b/Sources/WallpaperPeek/WallpaperEngine.swift
@@ -11,9 +11,12 @@ final class WallpaperEngine {
 
     static let supportedExt: Set<String> = ["jpg", "jpeg", "png", "gif", "webp", "bmp", "tiff", "heic"]
 
+    // 로컬 변경: Downloads/wallpapers -> Pictures/Wallpapers.
+    // 이 머신의 벽지가 거기 있고, 벽지 감시자(launchd.user.agents.wal-watch)도
+    // 같은 파일들을 보고 팔레트를 뽑는다.
     static var wallpaperDir: URL {
         FileManager.default.homeDirectoryForCurrentUser
-            .appendingPathComponent("Downloads/wallpapers")
+            .appendingPathComponent("Pictures/Wallpapers")
     }
 
     static let cacheDir: URL = {
