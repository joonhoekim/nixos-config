# amoled 프로필의 alacritty 조각. apps/rice-switch 가 ~/.config/alacritty/rice.toml
# 로 복사한다. 색은 여기 없다 — DMS/matugen 이 dank-theme.toml 에 따로 쓰고,
# alacritty.toml 이 둘 다 import 한다.
# 불투명 + 좁은 패딩. 검정 배경이 그대로 검정으로 보여야 한다.

[window]
opacity = 1.0

[window.padding]
x = 16
y = 16

[font]
size = 10
