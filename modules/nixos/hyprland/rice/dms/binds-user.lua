-- 사용자 키바인드 — DMS 설정 GUI 가 여기에 쓴다. hyprland.lua 가 자기 바인드를
-- 전부 건 *뒤에* 부르는 파일이다.
--
-- 하이프랜드는 같은 키에 걸린 바인드를 전부 실행한다. 이미 잡혀 있는 키를
-- 여기서 다시 잡을 거면 먼저 풀어야 한다 (키 문자열은 대소문자까지 정확히
-- 같아야 한다):
--
--   hl.unbind("SUPER + R")
--   hl.bind("SUPER + R", hl.dsp.exec_cmd("..."))
